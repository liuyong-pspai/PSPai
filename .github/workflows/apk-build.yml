name: Build APK (Full)

on:
  push:
    paths:
      - 'frontend/pwa/html/**'
      - 'mobile/android-app/**'
      - '.github/workflows/apk-build.yml'
    branches: [master]
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - uses: actions/setup-java@v4
        with: { distribution: 'temurin', java-version: '21' }
      - name: Setup Android SDK
        uses: android-actions/setup-android@v3
        with:
          packages: 'platforms;android-34 build-tools;34.0.0 platform-tools'

      - name: Download Pyodide runtime
        run: |
          echo "Downloading Pyodide full runtime..."
          mkdir -p frontend/pwa/pyodide
          # 从官方Pyodide Release下载（约350MB压缩包，解压后~700MB）
          curl -L --max-time 900 --retry 2 --retry-delay 10 \
            -o /tmp/pyodide.tar.bz2 \
            "https://github.com/pyodide/pyodide/releases/download/0.27.5/pyodide-0.27.5.tar.bz2" || {
            echo "⚠️ GitHub超时，尝试jsDelivr CDN..."
            curl -L --max-time 900 --retry 2 \
              -o /tmp/pyodide.tar.bz2 \
              "https://cdn.jsdelivr.net/gh/pyodide/pyodide@0.27.5/dist/pyodide-0.27.5.tar.bz2"
          }
          if [ -f /tmp/pyodide.tar.bz2 ] && [ -s /tmp/pyodide.tar.bz2 ]; then
            tar -xjf /tmp/pyodide.tar.bz2 -C frontend/pwa/
            rm /tmp/pyodide.tar.bz2
            echo "Pyodide ready:"
            du -sh frontend/pwa/pyodide/
          else
            echo "❌ Pyodide下载失败，构建轻量版（首次启动后台下载）"
            mkdir -p frontend/pwa/pyodide
            cp frontend/pwa/html/pyodide/pyodide.js frontend/pwa/pyodide/
          fi

      - name: Build APK (Full)
        run: |
          set -e
          mkdir -p app-build/www
          cp -r frontend/pwa/html/* app-build/www/
          # 全功能版：复制完整Pyodide运行时
          cp -r frontend/pwa/pyodide app-build/www/pyodide
          cp frontend/pwa/html/pyodide-bridge.js app-build/www/pyodide-bridge.js 2>/dev/null || true
          cp app-build/www/mobile.html app-build/www/index.html
          rm -f app-build/www/server.py
          echo "www files: $(find app-build/www -type f | wc -l)"
          du -sh app-build/www/
          cd app-build
          npm init -y > /dev/null 2>&1
          npm install @capacitor/core @capacitor/cli @capacitor/android
          npx cap init 小龙人 com.pspai.xiaolongren --web-dir=www
          npx cap add android
          # 配置App名和图标
          sed -i 's/<string name="app_name">.*<\/string>/<string name="app_name">小龙人<\/string>/' \
            android/app/src/main/res/values/strings.xml
          sed -i 's/android:usesCleartextTraffic="false"/android:usesCleartextTraffic="true"/' \
            android/app/src/main/AndroidManifest.xml 2>/dev/null || true
          # 复制图标
          for density in mdpi hdpi xhdpi xxhdpi xxxhdpi; do
            [ -f ${{ github.workspace }}/mobile/android-app/res/mipmap-${density}/ic_launcher.png ] && \
              cp ${{ github.workspace }}/mobile/android-app/res/mipmap-${density}/ic_launcher.png \
                android/app/src/main/res/mipmap-${density}/ic_launcher.png
          done
          # 网络安全配置
          mkdir -p android/app/src/main/res/xml
          cat > android/app/src/main/res/xml/network_security_config.xml << 'NSEOF'
          <?xml version="1.0" encoding="utf-8"?>
          <network-security-config>
              <base-config cleartextTrafficPermitted="false">
                  <trust-anchors>
                      <certificates src="system" />
                  </trust-anchors>
              </base-config>
              <domain-config cleartextTrafficPermitted="true">
                  <domain includeSubdomains="true">api.deepseek.com</domain>
                  <domain includeSubdomains="true">api.openai.com</domain>
                  <domain includeSubdomains="true">api.moonshot.cn</domain>
                  <domain includeSubdomains="true">open.bigmodel.cn</domain>
                  <domain includeSubdomains="true">dashscope.aliyuncs.com</domain>
                  <domain includeSubdomains="true">github.com</domain>
                  <domain includeSubdomains="true">objects.githubusercontent.com</domain>
              </domain-config>
          </network-security-config>
          NSEOF
          sed -i 's|<application|<application android:networkSecurityConfig="@xml/network_security_config"|' \
            android/app/src/main/AndroidManifest.xml
          # Kotlin fix
          cat >> android/app/build.gradle << 'GRADLEEOF'

          configurations.all {
              exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk7'
              exclude group: 'org.jetbrains.kotlin', module: 'kotlin-stdlib-jdk8'
          }
          GRADLEEOF
          SDK=$(echo $ANDROID_HOME $ANDROID_SDK_ROOT | tr ' ' '\n' | head -1)
          echo "sdk.dir=$SDK" > android/local.properties
          cd android
          ./gradlew assembleDebug --no-daemon --stacktrace 2>&1 | tail -30
          echo "--- APK size ---"
          ls -lh app/build/outputs/apk/debug/app-debug.apk || echo "APK NOT FOUND"

      - uses: actions/upload-artifact@v4
        with:
          name: xiaolongren-full-apk
          path: app-build/android/app/build/outputs/apk/debug/app-debug.apk

      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: PSP3.0
          files: app-build/android/app/build/outputs/apk/debug/app-debug.apk
          name: 小龙人 PSP3.0 全功能手机版
          body: |
            🐉 小龙人 PSP3.0 全功能手机APK
            - 完整Pyodide Python引擎（开箱即用）
            - 6大数字生命体角色
            - 多模型支持（DeepSeek/OpenAI/通义千问/智谱GLM/Anthropic）
            - 语音对话 + 文字聊天 + 文件上传
            - 修复发送按钮鸿蒙兼容性
            - config.html加返回按钮
          prerelease: true
          fail_on_unmatched_files: false
