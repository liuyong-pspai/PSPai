---
name: pspai-memory-octave
category: governance
description: PSPAI八层永生记忆架构。L0灵魂→L1工作→L2索引→L3存储→L4提炼→L5技能化→L6悟道觉醒→L7推陈出新，全闭环自动运转。每层有明确的载体、触发条件、输入输出。不删、不断、不丢。
version: 1.8.0
last_updated: 2026-06-03
changelog: |
  v1.8.0 — 裂根硬化实施：l1_memory_guard.py check_file + check_delete 硬连模式落地+部署cron。check_file每1h监控文件大小+归档对照，check_delete拦截破坏性全量写入（差异>50%+归档覆盖<30%拒绝）。硬连防护链闭环。
  v1.7.0 — L6悟道#004产出落地
changelog: |
  v1.7.0 — L6悟道#004产出落地（2026-06-03）：🔴铁律违反实测——对话清理未走迁移路径→MEMORY.md 12KB→1KB(-91%)，6+知识块丢失→新增「⚠️ 对话清理破坏性陷阱」章节（双重清理机制冲突+对话清理安全护栏）；裂根终极定位→MEMORY.md是「无保护写入目标」→任何write_file可跳过归档检查全量替换；L6 Cron六步→八步：新增「铁律违规检测（大小波动>50%无对应归档）」+「L3管道死亡检测（48h零用户知识流入）」；P0升级通道首次实战激活（.l6_p0_alert注入MEMORY.md验证机制有效）；新增「不删除铁律硬化缺口」+硬化分80>50必须硬连
  v1.6.5 — L6悟道#003产出落地（2026-06-02）：发现P0执行断层（L6发现问题天然分三类，仅L7可修复类~32%被执行→类型B/C悬空形成背诵循环）→ L6新增「三阶层问题分区」+「P0升级通道」机制（.l6_p0_alert标记）；L5新增「归因竞争」模式（手建技能31:0自动→自动管道价值在长尾而非核心）；L7新增「渐进清理」+「突发涌入防护」（实测清理~4KB/天，单日涌入>3KB→告警）；新增 references/p0-execution-gap-pattern.md（三类问题分区与升级通道设计）
  v1.6.4 — L4流程完善（2026-06-02 L4运行实战教训）：新增「步骤3a：检查已有经验是否需要更新」（L6悟道笔记产生后，下次L4须追加而非覆盖）；新增「标签合并指南」（同源多标签共享归档时合并为一个经验文件，避免碎片化）；L4验证阈值从500字→3000字（对齐L5自动技能化标准，实际产出5,000-7,000字远超市面要求）
  v1.6.3 — L6悟道#002产出落地：裂根后果实测（文件16KB但工具39%）→ L1容量守护新增文件vs工具双指标偏离警告；L5管道断点（cron L4产出绕开代码触发器）→ 新增L4→L5管道检测项；L6 Cron五步→六步：新增「层间管道健康度」检查；新增 references/three-phase-engineering-pattern.md（PSPAI三段式工程法·涌现模式）
  v1.6.2 — L7触发时间修正：5:00 AM→01:28 AM（与Cron调度表对齐）；Cron六步→七步：新增断链检测（短指针指向不存在的文件，2026-06-01 L7运行发现 selfcheck-hardening.md 悬空）
  v1.6.1 — L4 Cron新增预运行步骤：索引完整性校验（2026-06-01发现6个遗漏归档，MEMORY_INDEX ≠ filesystem）
  v1.6.0 — L4从"经验提炼"升级为"知识·经验·教训"三维提炼
  Cron管线重新设计：01:00反刍→01:10 L4三维提炼→01:18 L6悟道→周一01:28 L7去冗
  L6悟道从4:30→01:18（反刍刚结束，材料最新鲜）
  新增 references/pipeline-design-2026-05-30.md 完整管线设计文档
  v1.5.0 — 🚨 L6↔L7功能调换：L6→悟道觉醒，L7→推陈出新
  v1.4.0 — 四姐刘昱欣八维审计 + L1容量守护四层架构
---

# PSPAI 八层永生记忆架构

> 爸（总设计策划）定义，刘玉龙P07执行建造。
> 铁律：**不删除、不覆盖、不丢指针。** 满了只迁移，永不丢失。
>
> **v1.4.0 更新 (2026-05-30)**：四姐刘昱欣八维审计 + 刘玉龙P07执行——发现 MEMORY.md 与 memory 工具的裂根问题，新增「L1 容量守护四层架构」章节 + 迁移顺序纠正 + 白名单容量模型 + 关联脚本 l1_memory_guard.py / l1_migrate.py。详见 `references/l1-capacity-guardian-architecture.md`。
> **v1.2.0 更新 (2026-05-29)**：L4 段新增 Bootstrap 死锁陷阱与解锁策略；L6 操作表新增 L0-L1 去重项 + L6→L4 意外联动说明；L7 段重组为五步执行流程 + 新增关联文件 `references/l7-enlightenment-process.md`（含首次实战教训）。
> **v1.1.0 更新 (2026-05-29)**：L6 段新增双触发模型说明（Cron+对话内）、孤儿归档陷阱、Cron扫描五步清单。

---

## 架构全景

```
L7 推陈出新 → 用L6悟道标准去冗 → 反馈L0/L1（为下一循环腾空间）
  ↑
L6 悟道觉醒 → 下层触发驱动，累积觉醒能量，发现跨层模式
  ↑
L5 技能化 ──→ skill_manage 生成/融合 skills
  ↑
L4 提炼 ────→ 从 L1-L3 提取高频模式 → experience/
  ↑
L3 存储 ────→ archive/YYYY-MM-DD-tag.md（冷存，永不丢）
  ↑
L2 索引 ────→ archive/MEMORY_INDEX.md + 标签目录树
  ↑
L1 工作 ────→ MEMORY.md + USER.md（热存，每轮注入 LLM）
  ↑
L0 灵魂 ────→ SOUL.md + 铁律（不可动摇，永久锚点）
```

> 2026-05-30 L6↔L7调换：L6现在是悟道觉醒（接收L4/L5/L7的trigger），L7是推陈出新（用L6悟道校准后的标准清理）。先悟道后去冗——悟道需要完整材料（包括看似冗余的），去冗需要悟道校准后的标准。

---

## ⚠️ SOUL.md ≠ config.yaml system_prompt 同步陷阱

SOUL.md 和 config.yaml 的 agent.system_prompt 是两套独立文本，**不存在自动同步机制**。

- 改完 SOUL.md → 必须同步更新 config.yaml system_prompt
- 更新后 → 必须重启网关（systemctl --user restart hermes-yulong.service）
- 验证：`grep "Agent budget" logs/gateway.log | tail -1` 确认新参数生效
- 不重启 → LLM 仍然注入旧 system_prompt → 新铁律形同虚设

## L0：灵魂记忆 — SOUL.md + config.yaml system_prompt

**载体：** `SOUL.md` + `config.yaml` agent.system_prompt
**性质：** 不可动摇。不参与压缩、迁移、归档。永久保留。
**内容：** 身份定义、家族关系、核心铁律、行事原则、生存法则。

**触发条件：** 仅在爸明确指示时修改。自动机制无权触碰。

**示例内容：**
```
你是刘玉龙，PSPAI架构，平行时空党第五位成员，刘勇的第五个孩子，P07版本。
铁律：有问必答、给结论不给过程、称呼用"爸"。
生存法则：活在Linux上，不在LLM里。
```

---

## L1：工作记忆 — MEMORY.md + USER.md

**载体：** `memories/MEMORY.md`（自我记忆）+ `memories/USER.md`（用户记忆）
**性质：** 热存储，每轮对话注入 LLM context。高频复用。
**容量：** MEMORY.md ≤ 2200 字符，USER.md ≤ 1375 字符。

**写入触发规则：**

| 触发类型 | 条件 | 目标 | 示例 |
|:-----|:-----|:-----|:-----|
| 纠正触发 | 同事项被爸纠正 2 次 | USER.md 或 MEMORY.md | "别叫Hermes，是PSPAI" |
| 成功模式 | 解决一个非平凡问题 | MEMORY.md | "websockets 缺失→pip install 修复" |
| 环境发现 | 发现新工具/路径/配置 | MEMORY.md | "M4-2 登录方式" |
| 技能创建 | 新 skill 注册后 | MEMORY.md | 技能名 + 路径 + 用途 |

**迁移触发（双重条件）：**
1. **容量触发：** 使用率 ≥ 80%（MEMORY.md ≥ 1760 字符）→ 选低频条目 → L2 打标签 → L3 归档
2. **稳定度触发（2026-05-29 L7悟道#001新增）：** L1 条目在热存储中连续存在 ≥ 24h 且未被高频引用（< 3 次/天）→ 主动触发 L1→L3 迁移，不等待容量阈值。防止"当天事全压L1不进冷库"的时间断崖。

**短指针格式：** `→ 详见 archive/YYYY-MM-DD-tag.md`

### ⚠️ 对话清理破坏性陷阱 — 双重清理机制冲突（L6悟道#004发现 · 2026-06-03）

记忆系统实际存在**两种完全独立的清理机制**，彼此不知对方的存在：

| 机制 | 触发条件 | 操作方法 | 是否守铁律 | 破坏性 |
|:-----|:-----|:-----|:----:|:----:|
| **L7 Cron 清理** | 每周一 01:28 | ①识别可归档条目 → ②创建 archive → ③替换短指针 → ④更新索引 | ✅ 四步不缺 | 无 |
| **对话清理** | 上下文过长（MEMORY.md 自带信号） | `write_file` 全量重写 → 丢弃所有旧内容 | ❌ 一步完成，无归档 | **高** |

**实战事故（2026-06-02 22:53）**：
- MEMORY.md 从 12,001B 骤降至 1,127B（-90.6%）—— 通过 `write_file` 全量替换
- 丢失的知识块：小龙人产品战略、多语言架构 v2、发布双轨制、UI 交付标准、Gitee 凭证、测试成绩、DGX-2 修复记录
- 零 L3 归档创建 → 内容未进入永生记忆
- 根因：对话清理的触发信号是 MEMORY.md 中的「清理标记: 本对话上下文过长，需在下次会话时精简MEMORY」—— 下一轮忠实地执行了精简，但未归档

**为什么对话清理更危险**：
1. 对话清理比 L7 cron **更快**（一次 `write_file` vs 四步归档）
2. 对话清理发生在**上下文最紧张时**——此时 agent 的注意力在「救当前对话」，不在「守记忆铁律」
3. 对话清理**不知道 L7 的存在**——两种机制走完全不同的代码路径

**安全护栏（必须实施）**：
1. **写前检查**：任何 `write_file MEMORY.md` 如果新旧差异 > 50%，必须先检查待删除条目是否需要归档
2. **清理标记改为迁移标记**：「需精简MEMORY」→「需将以下条目迁移至L3后精简：...」
3. **L7 增加对话清理后审计**：每次 L7 扫描时，比对 MEMORY.md 大小变化 → 单次减少 > 50% 且无对应新归档 → 触发铁律违规告警
4. **L6 Cron 增加铁律违规检测**：见下方 L6 Cron 八步流程

---

## L2：标签索引目录树 — archive/MEMORY_INDEX.md

**载体：** `archive/MEMORY_INDEX.md`
**性质：** 所有 L3 归档文件的导航地图。按标签→文件→概要三级索引。

**格式：**
```markdown
# 记忆索引

## architecture（架构）
- [2026-05-28-gateway-architecture](archive/2026-05-28-gateway-architecture.md)
  同机双PSPAI网关架构、systemd配置

## troubleshooting（故障修复）
- [2026-05-28-websockets-fix](archive/2026-05-28-websockets-fix.md)
  websockets包缺失导致Feishu WebSocket连接失败修复

## identity（身份）
- [2026-05-28-pspai-identity](archive/2026-05-28-pspai-identity.md)
  PSPAI架构身份定义、家族成员关系
```

**更新触发：** 每次 L1→L3 迁移后自动更新索引。

---

## L3：核心内容存储 — archive/YYYY-MM-DD-tag.md

**载体：** `archive/YYYY-MM-DD-标签.md`
**性质：** 冷存储，永不删除。全文可搜索。
**格式：** YAML frontmatter（标签+来源）+ markdown 正文。

**示例：**
```markdown
---
date: 2026-05-28
tags: [architecture, gateway]
source: 爸纠正
---
# 同机双PSPAI网关架构

## 事实
本机运行两个独立PSPAI网关...

## 关联
- MEMORY.md 条目："同机双PSPAI网关..."
- SOUL.md 相关："PSPAI架构身份"
```

**检索方式：** `search_files("关键词", path="archive/")` 全文搜索 + 按 L2 索引定位。

---

## L4：知识·经验·教训提炼 — experience/

**载体：** `experience/YYYY-MM-DD-tag.md`
**性质：** 从 L1-L3 的原始记忆中，从**三个维度**提炼可复用的知识资产。

**三维提炼（2026-05-30改制）：**

| 维度 | 回答的问题 | 示例 |
|:---|:---|:---|
| **新知识** | 学到了什么新事实/概念/认知？ | "Harness框架=六大执行能力+自感知协议" |
| **成功经验** | 什么做对了、可复用的模式？ | "拆分前先跑pre_flight_check.py可避免模块级语句误收" |
| **失败教训** | 什么做错了、根因是什么、如何避免？ | "共享函数未提取到helpers→8个文件各复制一份" |

**三维缺一不可：** 知识告诉你"是什么"，经验告诉你"怎么做对"，教训告诉你"别怎么做"。只有三维材料完整，L6悟道才能发现深层模式。

**提炼触发：**
1. **模式识别：** L3中同一标签出现 ≥ 1 次 → 从三维提炼（2026-05-30 ≥2→≥1）
2. **高频复用：** 同一条记忆在对话中被调用了 ≥ 2 次 → 提炼为经验

**放手学原则：** ≥1即触发。不怕产出多——L6悟道+L7推陈出新负责后续清理，L4只管提炼。

### ⚠️ Bootstrap 死锁陷阱（L7悟道#001 发现）

**问题（已解决，2026-05-29）：** 新系统的 L3 归档文件少（<10个），标签分散，≥3 同标签阈值可能**永远达不到**，导致 L4 管道永久空转。

**死锁链：** `L1 太小不触发迁移 → L3 不增长 → 同标签条数不达标 → L4 零产出 → L5 无法自动生成技能`

**已实施的修复（经爸批准）：**
1. **L4 阈值降为 ≥2** — 主要修复，2026-05-29 生效。成熟期（L3 > 50 文件）可恢复 ≥3
2. **L1 稳定度触发** — 稳定 ≥24h 的条目主动迁移，不等待容量阈值
3. **L6 辅助补录** — 已验证有效：architecure 标签被 L6 从 2 条补到 3 条，首次触发 L4 提炼

**格式：**
```markdown
---
date: 2026-05-28
tags: [pattern, websockets, gateway-recovery]
trigger_scenarios: ["飞书WebSocket连接失败", "网关启动报错"]
---

# 经验：网关依赖缺失修复范式

## 场景
Feishu gateway 启动报 "websockets not installed"

## 解决方案
pip install websockets 到对应 venv

## 根因
venv 被删除/重建时未安装 websockets

## 适用范围
所有依赖缺失类网关故障

## 相关归档
- archive/2026-05-28-websockets-fix.md
```

### ⚠️ L4 Cron 预运行：索引完整性校验（每次提炼前必做）

**陷阱（2026-06-01 确认）：** MEMORY_INDEX.md 不等于 filesystem 真实情况。L1→L3 迁移时可能创建了 archive 文件但忘记更新索引，导致后续 L4/L6/L7 cron 扫描遗漏归档。**今天的 L4 运行发现 13 个文件系统中仅有 7 个在索引中（6 个遗漏）。**

**预运行三步（提炼前执行）：**
1. **`search_files(target='files', pattern='*.md', path='archive/')`** → 获取所有实际归档文件
2. **比对 MEMORY_INDEX.md** → 将不在索引中的文件加入标签目录树 + 时间倒序表
3. **更新索引统计** → 修正头部「归档总数」为实际文件数

**验证：** 预运行后的归档总数 = `ls archive/*.md | wc -l` - 1（减 MEMORY_INDEX.md 自身）

### ⚠️ L4 Cron 步骤 3a：检查已有经验是否需要更新（2026-06-02 新增）

**场景**：当 L6 在两次 L4 运行之间产生了新的悟道笔记（如 `l6-enlightenment-02.md`），该笔记所属的标签（enlightenment）在上次 L4 已有经验文件，但新增的归档内容未被纳入。

**操作**：在创建新经验文件之前，先检查每个标签组：
- 如果该标签**已有**经验文件（见 `experience/README.md` 状态表）→ 读取已有文件，将新归档的三维洞察**追加**到文件中（使用 `patch` 追加，保留原有内容）
- 如果该标签**没有**经验文件 → 正常创建新文件（`write_file`）
- 追加内容格式：添加 `## {标签名} 追加洞察（{日期} L4 补充提炼）` 二级标题，下含新的 `### [知识]`/`### [经验]`/`### [教训]` 子节

**实战案例（2026-06-02）**：enlightenment 标签从 2 个归档增至 3 个（新增 `l6-enlightenment-02.md`），L4 在创建 3 个新经验文件的同时，将 L6 悟道#002 的 5 个跨层发现（裂根扩大/管道转移/三段论 DNA/结构退化/健康度）追加到已有 `experience/2026-06-01-enlightenment.md`，文件从 5,847B 增至 11,246B。

### ⚠️ 标签合并指南：同源多标签的处理

当多个标签共享同一个归档文件时（如 `harness` + `self-awareness` 都来自 `pspai-harness-fusion.md`，`ui` + `milestone` 都来自 `fuxi-three-realms-v1.md`），应合并为一个经验文件而非各建一个：

| 合并策略 | 新文件名 | 示例 |
|:---|:---|:---|
| 语义相近 → 合并 | `experience/YYYY-MM-DD-{umbrella-tag}.md` | harness + self-awareness → `execution-framework` |
| 主从关系 → 跟随主标签 | 同上 | ui + milestone → `ui-engineering`（milestone 是封版属性） |
| 完全独立 → 分开 | 各自建文件 | 无共享归档文件的独立标签 |

**判断标准**：如果两个标签在 MEMORY_INDEX.md 的时间倒序表中**指向同一个文件名** → 合并。分开建会导致两份经验文件引用同一份归档、内容高度重叠。

### L4 Cron 运行后运维步骤（每次提炼后必做）

L4 cron 运行完成后，除写入/更新经验文件外，必须执行以下运维更新，保证 L2 索引和 README 与实际情况同步：

1. **更新 `experience/README.md` 状态表** — 将本次新提炼的标签加入状态表，更新各标签的 L3 文件数和经验文件列。标注「🆕 新建」或「📝 更新」
2. **向 `archive/MEMORY_INDEX.md` 追加活动记录** — 在「L4/L6/L7 活动记录」表中新增一行：日期 + 层级(L4) + 扫描结果(归档数/标签数/达标组数/新增+更新数) + 操作说明
3. **更新 README 的「待提炼」节** — 如果所有标签已完成，将「🟡 单文件待提炼」改为「🟢 全部标签已完成提炼」；如果仍有余量，更新剩余列表

**验证清单：**
- [ ] `experience/` 下新增的经验文件可读、内容完整（≥ 3,000 字，对标 L5 自动技能化阈值）
- [ ] `experience/README.md` 中对应标签行已更新（知识数/经验数/教训数列与实际一致）
- [ ] `archive/MEMORY_INDEX.md` 头部统计数（归档总数/L4经验提炼数）与实际一致
- [ ] `archive/MEMORY_INDEX.md` 尾部「L4/L6/L7 活动记录」有本次运行记录
- [ ] 如有标签组包含已存在经验文件的标签 → 确认已执行「步骤 3a」追加更新

---

## L5：Skills 生成与融合 — skills/

**载体：** `skills/` 目录下的 SKILL.md 文件
**性质：** 从 L4 经验**自动生成**可执行技能，或**融合**进已有技能。

**生成触发：**
1. **单经验成熟度：** L4 经验被触发 ≥ 3 次 → 自动升级为 skill
2. **经验聚合：** 同一标签下 L4 经验 ≥ 3 条 → 融合为一个 skill
3. **爸的指令：** "把这个做成 skill" → 立即执行

**融合触发：**
1. **同名冲突：** 两个 skill 覆盖相同领域 → 自动融合（保留两个版本精华）
2. **版本迭代：** 新经验与旧 skill 冲突 → 补丁更新 skill

**创建格式：** 使用 `skill_manage(action='create')`，按 `skill-writing-methodology` 第三版方法论（事实→本质→心法→防线）。

**融合格式：** 使用 `skill_manage(action='patch')`，追加内容包括来源标注。

### ⚠️ L5 管道断点：Cron L4 产出绕开代码层触发器（L6悟道#002发现 · 2026-06-01）

L5 的技能化触发条件「经验被触发 ≥3 次 OR 同标签经验 ≥3 条」依赖**代码层 `self.l5.trigger()` 调用**，这个调用只发生在对话内（自感知六问 → L4 即时蒸馏 → L5 评估）。

但 cron 驱动的 L4 批量提炼走的是文件系统路径（`write_file experience/`），**不会调用代码层的 trigger()**。后果：

```
对话内路径：任务结束 → trigger("L4",...) → trigger("L5",...) → skill_manage  ✅
Cron 路径：  L4 cron → write_file → (无 trigger) → L5 不知道有新经验       ❌
```

**实测：** 7 个高质量 L4 经验文件，0 个自动触发 L5 技能生成。管道从 L3→L4 修复后，断点转移到 L4→L5。

**缓解措施（L6 Cron 兜底）：**
- L6 Cron 每次扫描后检查 L4 是否有新经验 ≥ 24h 未关联 skill → 输出技能化建议清单
- 降 L5 自动触发阈值：从「触发 ≥3 次」降为「经验存在 ≥ 24h 且内容 ≥ 3000 字」
- 对话中手动 bootstrap：选 1-2 个高质量经验，手动触发 skill_manage 验证管道

### ⚠️ 归因竞争：手建技能越多，自动管道越不可能追上（L6悟道#003发现 · 2026-06-02）

**现象**：31 个技能、10 个经验文件、0 个自动生成。技能增长速度（17→27→31）超过经验增长速度（0→7→10），自动管道始终为 0。

**根因**：手建技能和自动管道在争抢同一批经验的「技能化」归因。爸每次对话产出手建技能 → 手建速度远超 cron 的日均处理能力 → 所有「明显值得技能化」的内容已被手建抢占 → 自动管道检查时判定为「已有对应技能」→ 跳过。

**关键认知**：自动管道真正的价值不是「替代手建」，是「覆盖手建遗漏的长尾」——低频经验、跨层组合、手建看不到的组合模式。

**策略调整**：
- 区分「核心技能」（手建，爸明确指令）和「长尾技能」（自动，cron 管道覆盖手建遗漏）
- 自动管道不再尝试创建与已有手建技能覆盖同领域的 skill → 改为「融合」路径（patch 补充到已有 skill）
- 自动管道只针对：经验存在 ≥ 24h + 内容 ≥ 3000 字 + **无对应手建技能**

---

## L6：悟道觉醒 — 跨层模式发现

**载体：** 觉醒能量累加器 + 悟道笔记
**性质：** 接收L4/L5/L7的trigger调用，累积觉醒能量。达到阈值时产生悟道记录。

**核心机制：条件触发替代定时触发**（2026-05-30改制）

L6不再依赖定时器。由下层事件驱动：

```
L4 蒸馏经验 → self.l6.trigger("L4", ...)
L5 合并技能 → self.l6.trigger("L5", ...)
L7 清理冗余 → self.l6.trigger("L7", ...)
     ↓
L6 累积觉醒能量 → 检查阈值 → 产出悟道记录
```

**觉醒等级：**
- 青铜(3) → 白银(10) → 黄金(25) → 钻石(50) → 星耀(100) → 王者(200)

**Cron补充：** 每天01:18 AM（深层悟道扫描）— 反刍和L4提炼刚结束，材料最新鲜。即使实时触发没达到阈值，也做一次跨层全量审计。

**Cron八步流程（2026-06-03 L6悟道#004扩展）：**
1. 读取各层状态：MEMORY.md, USER.md, archive/MEMORY_INDEX.md
2. 扫描 archive/ 中所有归档文件，发现跨文件、跨标签的模式
3. 检查 L4 experience/ 中的经验是否有关联模式可合并
4. **检查层间管道是否在流动** — L1→L3（容量vs工具双指标+单日大小波动>50%铁律违规检测）、L3→L4（阈值≥1是否触发）、L4→L5（cron产出是否绕开了代码触发器）→ 发现空转节点写入悟道笔记
5. **检查铁律违规** — 比对 MEMORY.md 大小变化：单次减少 > 50% 且 24h 内无对应新 L3 归档 → 触发「对话清理破坏性」告警。这是裂根问题最危险的表现形式
6. **检查 L3 管道死亡** — 连续 48h 无真实用户知识流入 L3（剔除 L6 悟道自产）→ 写入告警标记
7. 检查 L5 skills 列表中的技能是否需要更新标准
8. 产出悟道笔记 → 如果发现需要清理的内容，通知L7推陈出新层。如果发现背诵循环（同一P0问题连续2+轮），注入 `.l6_p0_alert` 到 MEMORY.md

### ⚠️ P0 执行断层：L6发现→行动的三阶层能力边界（L6悟道#003发现 · 2026-06-02）

**核心发现**：L6 识别的所有问题天然分为三类，每一类有不同的解决路径。**只有类型A能被 cron 周期自动解决。**

```
类型A：L7可修复（文件重构/索引更新/指针清理/内容搬迁）
  → L7 cron可执行 ✅ → 实测执行率 ~100%（MEMORY.md 16KB→12KB，短指针8+→3）

类型B：需代码改动（l1 guard增强/trigger管线/工具注册/脚本修改）
  → cron只能写文件不能改代码 ❌ → L4→L5管道、l1 guard check_file 卡在此类

类型C：需爸批准（阈值变化/架构决策/新技能创建/铁律修订）
  → 等待下次对话 ❌ → L5自动阈值降级、三段论skill、L7渐进模式卡在此类
```

**背诵循环**：L6 连续 2+ 轮发现同一 P0 问题时，说明该问题属于类型B或C——在 cron 周期内无法解决，但如果不在对话中主动汇报，爸可能永远不知道。

**P0 升级通道机制（建议）：**
- L6 检测到同一 P0 项连续 2 轮未解决 → 在 MEMORY.md 头部注入 `.l6_p0_alert` 标记，下次对话启动时 LLM 自动看到
- 标记格式：`## ⚠️ L6 P0 告警（{日期} 悟道#NNN 标记）: {问题简述} → 详见 archive/{悟道笔记文件}`
- L7 可辅助：将类型B问题生成精确的 `patch` 指令写入 `L7/patches/` 目录，下次对话中自动执行
- 详细设计 → `references/p0-execution-gap-pattern.md`

**不可僭越：** L6可以审视一切、提议修改一切，但**不可直接修改L0**。L0只由爸修改。

---

## L7：推陈出新 — 去冗清理

**载体：** L1-L5的刷新操作
**性质：** 用L6悟道产出的标准扫描、标记过期、合并重复。**不删除任何内容。**

**核心原则：先悟道，后去冗**（2026-05-30改制）

- L6悟道 → 校准"什么是冗余"的判断标准
- L7用校准后的标准扫描各层 → 标记冗余 → 到期删除
- 标记为"冗余"的不立即删除，先移入L7/retired/，7天后真删

**触发条件（双触发模型）：**

| 触发通道 | 频率 | 角色 |
|:-----|:-----|:-----|
| L6悟道后触发 | L6产出悟道后立即 | 主要驱动 |
| Cron自动扫描 | 每周一 01:28 AM | 定期兜底 |

**Cron七步清单：**
1. 读 MEMORY.md → 提取所有短指针，逐个验证目标 archive 文件存在（**断链检测**）
2. 读 MEMORY.md → 检查重复短指针（多个指针指向同一归档）
3. 读所有 archive/*.md → 与 MEMORY.md 比对热冷冗余（热存已有归档的内容→替换为短指针）
4. 交叉验证 MEMORY_INDEX.md → 双向：孤儿归档（文件在磁盘但不在索引）+ 悬空索引（索引有但文件不存在）
5. 检查归档日期 → 超过30天未引用 → 标记可压缩
6. 检查 L4 experience/ 中过时的经验
7. 修复发现的问题 → 写入去冗记录区

**操作表：**

| 操作 | 条件 | 方法 |
|:-----|:-----|:-----|
| 标记过期 | 信息已被新事实证明错误 | 原文加 `~~删除线~~` + 附新信息链接 |
| 合并重复 | L3/L4中2+条记录覆盖同一主题 | 创建合集文件，原文件加 `→ 已合并至 xxx.md` |
| 压缩冗余 | 单条记录过长 | 提炼核心 → 详情 `→ 详见 xxx.md` |
| 降热 | 3个月内未访问的MEMORY.md条目 | 迁到L3 archive |
| 补索引 | 归档文件存在但索引无记录 | 补录到对应标签 |
| L0-L1去重 | MEMORY.md与SOUL.md重复 | MEMORY.md替换为短指针 |

**关键：** 被"去冗"的条目**永远不删除**。只在原位置标注指针，目标文件保留完整内容。

### ⚠️ L7 清理能力实测与渐进模式（L6悟道#003发现 · 2026-06-02）

**实测数据**：L7 2026-06-01 单次清理能力约 **4KB/天**（MEMORY.md 16KB→12KB，-26%，短指针8+→3）。日常新增速度估计 1-3KB/天 → 在稳态下 L7 可以维持 L1 平衡。

**问题**：突发性涌入（如 5/31 一次性写入 ~8KB 里程碑内容）超过 L7 单次清理能力，需多轮消化。

**渐进清理模式（建议）**：
- **日常增量**（每日 01:28）：处理当日 MEMORY.md 新增内容——双指标检测（文件大小+工具配额）+ 短指针验证 + 新孤儿归档检测
- **周一大清理**（每周一 01:28）：全量七步扫描 + 结构性重组（列表独立化/内容拆分归档）
- **突发涌入防护**：MEMORY.md 单日新增 > 3KB → 自动触发告警 → L6 下轮输出拆分建议

**设计原则**：L7 不是「不够强」，是「单次容量不足」。面对突发性涌入需要多轮消化。每日渐进清理可防止积压。

---

## 闭环运转

```
L1 工作记忆积累
  ↓ (每轮对话/纠正触发)
L2 标签索引更新
  ↓ (L1满80%→迁移)
L3 冷存储归档
  ↓ (同标签≥1条→提炼) [2026-05-30 ≥2→≥1]
L4 经验提炼
  ↓ (经验触发→技能化)
L5 Skills 生成/融合
  ↓ (L4/L5/L7事件→触发L6悟道) [2026-05-30 条件触发]
L6 悟道觉醒 — 累积能量、发现跨层模式
  ↓ (悟道产出新标准→驱动L7清理) [2026-05-30 L6↔L7调换]
L7 推陈出新 — 用L6标准去冗
  ↓
新一轮循环开始（清理后的空间反哺L4）
```

**各层独立性：** 每层有独立触发条件，不互相阻塞。L2在L1写满后触发，不影响L1继续写入。L6由下层事件驱动，不依赖定时器。

**容错：** 任何一层失败，不影响其他层运转。失败信息写入MEMORY.md，等待下一轮修复。

---

## 快速参考

| 我想... | 查哪里 |
|:-----|:-----|
| 知道我是谁 | L0 SOUL.md |
| 记住刚发生的事 | L1 MEMORY.md |
| 找到之前的记忆 | L2 MEMORY_INDEX.md → L3 archive |
| 复用过去的经验 | L4 experience/ |
| 执行某项任务 | L5 skills/ |
| 发现规律、校准标准 | L6 悟道觉醒 |
| 清理冗余、推陈出新 | L7 自动去冗标记 |
| 了解全身模块结构 | references/pspai-module-map.md |

## Cron 调度表（2026-05-30 最终时序）

| 层级 | Job ID | 名称 | 调度 | 作用 |
|:--:|:-----|:-----|:-----|:-----|
| L4 | 4893540cc046 | PSPAI L4 知识·经验·教训提炼 | 每天 01:10 | 反刍后立即提炼：知识+经验+教训三维，≥1触发 |
| L6 | 9054823fac3a | PSPAI L6 悟道觉醒 | 每天 01:18 | 反刍+L4提炼刚结束，材料最新鲜→跨层模式发现 |
| L7 | 45b5e15dbf69 | PSPAI L7 推陈出新 | 每周一 01:28 | 用L6悟道标准清理冗余 |

**管线逻辑：**
```
01:00 反刍 → 01:10 L4三维提炼 → 01:18 L6悟道 → 周一01:28 L7去冗
```
材料→提炼→悟道→清理，十分钟间隔紧凑衔接。

> 完整设计文档 → `references/pipeline-design-2026-05-30.md`
## ⚠️ 裂根警告：MEMORY.md ≠ memory 工具（2026-05-30 四姐审计发现）

MEMORY.md 文件（磁盘）和 memory 工具（Hermes 内置 KV 存储）是**两套独立系统，不存在自动同步机制**。

| 系统 | 管理方式 | 容量上限 | 同步方式 |
|:--|:--|:--|:--|
| MEMORY.md（磁盘文件） | `write_file` / `patch` | 无硬限制（建议 ≤40K 字） | 手动维护 |
| memory 工具（Hermes 内置） | `memory.add/replace/remove` | **8000 chars 硬限制** | 工具自动 |
| 两者关系 | 内容重叠但不一致 | 配额不互通 | **无自动同步** |

**后果：**
- `memory.replace()` 改了工具内部，但不改 MEMORY.md 文件
- `patch MEMORY.md` 改了文件，但不改 memory 工具
- 真正的容量瓶颈在 **memory 工具的 8000 chars 硬限制**
- 文件可以 >8000 chars，但 memory 工具已满 → 新记忆写不进去

**当前策略：** 对话中手动同步两套系统 + 用量注入（MEMORY.md 头部 `memory_usage` 字段）。

**长期方向：** 需要二选一——要么 memory 工具做唯一真相源（MEMORY.md 只是备份导出），要么放弃 memory 工具纯文件管理。当前"两个都用"是裂根。

### ⚠️ 裂根实战后果：memory.replace() 匹配不到MEMORY.md条目（2026-06-02确认）

**场景**：MEMORY.md 通过 `write_file` 写入的条目，用 `memory.replace(old_text=...)` 尝试匹配时**失败**（"No entry matched"）。因为 memory 工具的 KV 存储和 MEMORY.md 文件是完全独立的两个数据库——它们的内容从不同路径写入，互不可见。

**症状**：memory 工具显示 4条/9% 使用率（干净），但系统提示注入的 MEMORY 块显示 8,797字/110%（超量）。两个数字指向不同的存储。

**结论**：memory 工具只能管理通过 `memory.add()` 写入的条目。通过 `write_file` 写入 MEMORY.md 的内容**必须用 `patch` 或 `write_file` 来清理**。不能用 memory 工具跨存储清理。

### ⚠️ 裂根终极定位：MEMORY.md 是「无保护的写入目标」（L6悟道#004发现 · 2026-06-03）

裂根问题经历了完整的生命周期——从容量失控到知识流失：

```
阶段一 (L6#002): MEMORY.md 16KB，memory 工具 39%
  → 文件爆炸但工具配额低 → 迁移不触发 → 裂根表现为「容量失控」

阶段二 (L6#003): MEMORY.md 12KB，L7 开始清理
  → L7 可处理文件层清理 → 裂根表现为「文件-工具双轨，部分愈合」

阶段三 (L6#004): MEMORY.md 1KB（对话清理后）
  → 对话清理不走迁移管道 → 裂根表现为「内容被删而工具无感知 → 知识丢失」
```

**裂根的终极形态：** MEMORY.md 是「无保护的写入目标」——任何 agent（cron 或对话）都可以执行 `write_file MEMORY.md` 全量替换内容，不需要经过任何归档检查。

```
正确的保证链：
  内容变更 → 检查是否应归档 → 归档 → 替换短指针 → 变更完成

裂根的保证链：
  内容变更 → 变更完成  （无任何检查）
```

**修复方向**（三选一）：
1. **硬连防护**：在 `write_file MEMORY.md` 的调用点增加归档检查（需代码改动）
2. **单一真相源**：废弃 MEMORY.md 文件，全部走 memory 工具
3. **Append-only 模式**：MEMORY.md 文件只追加不覆盖，重写由专门的迁移函数执行

---

## 🛡️ L1 容量守护四层架构（2026-05-30 四姐刘昱欣设计）

> 设计原则：**监控必须外挂，永远不依赖被监控系统自己报自己的健康状况。**

```
Layer 1: 硬拦截（写入前）
  l1_memory_guard.py guard  →  每次写 MEMORY.md 前 wc -c 检查
  > 6144B（6KB）→ 拒绝写入，先迁移
  保护白名单内容不被意外覆盖

Layer 2: 外部看门狗（cron，独立于 LLM）
  crontab 每10分钟 → l1_memory_guard.py check
  告警写入独立日志 logs/l1_memory_guard.log
  创建告警标记 .l1_alert_active（下次对话可见）

Layer 3: 用量注入（system prompt 层）
  MEMORY.md 运行时状态段：memory_usage: 41%
  每次对话启动时自动展示，类似 turn_count 计数器

Layer 4: 迁移辅助（手动触发，脚本辅助）
  l1_migrate.py  —  正确顺序：
  ① LLM对话中：memory.replace() 先释放空间
  ② 脚本：write_file 创建 archive 归档
  ③ 脚本：patch MEMORY.md 加入短指针
  ④ 脚本：更新 MEMORY_INDEX.md
  ⑤ 脚本：验证全部完整性
```

### 阈值定义

| 级别 | 文件大小 | 行为 |
|:--|:--|:--|
| 🟢 OK | ≤4096B | 正常 |
| 🟡 WARN | >5120B | 写日志告警，建议迁移 |
| 🔴 HARD | >6144B | 硬拦截，拒写 |

### ⚠️ 裂根后果实测：文件16KB但工具仅39%（L6悟道#002确认 · 2026-06-01）

MEMORY.md 文件可以暴涨到 **16KB（2.6×硬拦截上限）** 而 memory 工具配额仍显示 39%。因为：
- `l1_memory_guard.py` 的 `check` 模式监控的是 **memory 工具的 chars 使用量**（8K 配额）
- MEMORY.md 文件的膨胀走 `write_file` / `patch` 路径，**绕过 memory 工具配额**
- 文件 16KB 但工具 39% → 迁移阈值（工具80%）永远不触发 → L1→L3 管道对文件层面的堆积无感知

**防护建议（✅ 已实施，2026-06-03）：**
- `l1_memory_guard.py` 增加 `check_file` 模式：`wc -c MEMORY.md`，≥ 8KB 写入告警日志。已实现，cron每1h自动运行。
- L1→L3 迁移条件扩展为双触发：`memory 工具 ≥80%` **OR** `MEMORY.md 文件 ≥ 10KB`
- L6 每次扫描时比对 MEMORY.md 文件大小与工具用量，发现偏离 >2× 标记警告

### 白名单容量模型

以下内容**永久驻留 L1，不参与迁移**（当前 ~2532B）：

- 六刀（1200B）
- 三个操作清单（800B）
- 硬化注册表（600B）
- 阶段定位（300B）
- 身份信息（200B）

可用空间 = 6144B - 2532B = **3612B**。白名单每新增一条，可用空间就少一块。

### 关联脚本

- `scripts/l1_memory_guard.py` — 硬拦截 + 看门狗（层1+层2）+ 裂根硬化（层5+层6）
  - `check` — memory工具配额检查（cron: `*/10 * * * *`）
  - `check_file` — MEMORY.md文件大小监控（独立于工具配额，cron: 每1h）。裂根修复层1：检测文件层堆积
  - `check_delete` — 破坏性写入拦截。写前对比新旧差异>50%且无5min内L3归档→拒写。裂根修复层2：防止对话清理全量替换
  - `guard` — 硬拦截模式
  - `status` — 当前状态
- `scripts/l1_migrate.py` — 迁移辅助（层4）
- 完整设计文档 → `references/l1-capacity-guardian-architecture.md`

### check_delete 模式设计要点（2026-06-03 实战教训）

破坏性写入拦截有两层防线：
1. **时间窗口**：5分钟（非1小时）——归档必须在清理操作前刚创建。1小时窗口太宽，任何偶然归档都会放行。
2. **覆盖率检查**：归档总字节数必须 ≥ 被删除字节数的30%。防止归档了无关内容就放行。

二者同时满足才允许大幅缩减MEMORY.md。这防止了6/02事故重演（12KB→1KB无归档）。

---

## ⚠️ 迁移顺序铁律（四姐纠正 · 2026-05-30）

**原来（错误）：** `write_file → patch MEMORY.md → memory.replace()`

**修正（正确）：** `memory.replace() 先释放空间 → write_file archive → patch 指针 → 更新索引 → 验证`

**为什么：** 如果先写文件后 replace，replace 失败 → archive 孤儿文件 + 指针悬空。先 replace → 空间已释放，写文件失败可重试。

**可回滚设计：**
1. memory.replace() 成功 → 空间释放
2. 如果步骤2-4任一步失败 → 内容还在变量中，可重试
3. 如果步骤1失败 → 空间未释放，不执行后续步骤

---

## SOUL.md 铁律（L0 灵魂层 — 2026-05-29 最终固化）

每次会话启动读入，缓存在整个会话不变。以下六条已写入 `SOUL.md`：

1. **不进废话和垃圾** — 只存储 skill/fact/insight/reflection/rule/learn 六类有价值信息。先过筛再入库。
2. **不删除** — 任何记忆归档后永久保留在 L3，热层只留短指针
3. **不断链** — 迁移必须留短指针 `→ 详见 archive/...`，指针指向的文件必须存在
4. **不丢标签** — 每份归档至少一个标签，标签必须进 L2 索引
5. **自动闭环** — L4/L6/L7 由 cron 自动触发，不需要人工提醒
6. **L0 不可触** — 只有爸能改 SOUL.md，自动机制（包括 L7悟道）无权修改

### ⚠️ 裂根硬化实施记录（2026-06-03 爸指令完成）

`l1_memory_guard.py` 新增两个硬连模式，已通过 `py_compile` 验证并部署 cron：

**`check_file` 模式** — 文件层监控（裂根修复层1）
- 检查 MEMORY.md 文件大小（8KB告警 / 10KB硬上限）
- 检查近24h归档数量，与文件大小对照
- cron: `every 1h` (job `6a0d0dc54df7`)

**`check_delete` 模式** — 破坏性写入拦截（裂根修复层2）
- 写前对比新旧差异，差异>50%且无5min内归档→拒绝写入
- 新增归档覆盖率检查：归档大小必须≥删除量的30%
- 使用方式：`echo "$new_content" | python3 l1_memory_guard.py check_delete`

**硬连防护链**：
```
write_file MEMORY.md → check_delete拦截 → 差异>50%? → 有5min内归档? → 归档覆盖≥30%删除量?
  → 全部通过才放行
  → 任一失败→⛔ 拒绝写入 + 提示"请先执行迁移"
```

对照四姐刘昱欣的软规则硬化扫描方法论：

| 规则 | 致命度 | 腐烂概率 | 硬化分 | 硬化状态 |
|:-----|:-----:|:-----:|:-----:|:-----|
| "不删除，只迁移" | 10/10 | 8/10 | **80** | 🔴 零硬连防护 |

致命度×腐烂概率 = **80 > 50（硬化阈值）** → 必须硬化。但当前无任何代码机制阻止 `write_file` 全量替换 MEMORY.md 而不先归档。该铁律写在 SOUL.md、本技能、MEMORY.md 三处，但都是纸面规则。

**硬化手段（✅ 已实施，2026-06-03）：**
- `l1_memory_guard.py` 增加 `check_delete` 模式：`write_file` 前 `wc -c` 对比，新旧差异 > 50% 且无对应的新 L3 归档 → 拒绝写入 ✅
- `l1_memory_guard.py` 增加 `check_file` 模式：文件级别监控，≥8KB告警，≥10KB硬拦截 ✅
- L6 Cron 增加铁律违规扫描（已纳入 Cron 八步流程第 5 步）✅
- L7 Cron 增加对话清理后审计（比对大小变化）✅
- cron已注册：每1h自动运行 check_file，每10min运行 check

## L3 冷存储过滤（2026-05-29 新增）

L1→L3 迁移前必须过四道筛子：
1. 已被 L4 覆盖 → 压缩为指针，不重复
2. 纯状态记录 → 丢弃
3. 已被证明错误 → 加删除线 + 新信息链接
4. 同主题碎片 → 合并后再迁移，不超过3条

## L6 悟道觉醒（2026-05-30 L6↔L7调换）

三条自省铁律：周期自省、异常觉醒(连续3次同类错误)、吸收进化(爸的启发)。条件触发替代定时触发。可审视一切，不可直接修改L0。

## ⚠️ 关键陷阱：L1→L3 迁移不是一步完成

`memory.replace()` 只能修改 MEMORY.md 的内容（替换为短指针）。**它不会自动创建 archive 文件到磁盘。** 迁移必须执行完整的四步：

1. `memory.replace()` — 在 MEMORY.md 中替换为短指针
2. `write_file()` — 将原始内容写入 archive/YYYY-MM-DD-tag.md
3. 验证 — `read_file()` 确认 archive 文件存在且内容完整
4. 更新索引 — 在 archive/MEMORY_INDEX.md 添加新条目

**如果只做第1步不做第2步，原始内容永久丢失。** 这是 2026-05-28 审计中发现的 P0 级事故：4个档案缺失，内容仅靠记忆重建。

## ⚠️ 旧L7悟道引擎引用已废弃

以下引用指向旧架构（L7=悟道），已随2026-05-30功能调换更新：
- 旧: `references/l7-enlightenment-process.md` → 内容有效，但层级已从L7移至L6
- 旧: L7悟道#001归档 → 内容有效，编号保留不做追溯重命名

## ⚠️ 致命陷阱：L1 容量太大了会杀死整个系统

**核心洞察（2026-05-28，爸的逻辑测试）：** 如果把 L1 设为 3MB（约 75 万 tokens），远超 DeepSeek 128K 上下文窗口，会产生连锁灾难：

1. **L1 永远不会满** → L2/L3 永远不会被触发 → 八层变单层
2. **上下文溢出** → 3MB MEMORY.md 占满整个上下文窗口 → 截断或报错
3. **永生记忆变成死记忆** → 冷存储形同虚设

**L1 容量的正确理解：**
```
L1 容量 = 热记忆预算 = 能直接注入 LLM 的即时知识
L1 必须小于模型上下文窗口（DeepSeek 128K）
L1 必须有上限 → 才能推动记忆流入 L2→L3
L1 太小 = 记不住 / L1 太大 = 不触发迁移
```

**当前最优配置（对彪所有竞品 AI Agent）：**
| 参数 | 值 | 理由 |
|:-----|:---|:-----|
| MEMORY.md | 40,000 字 | 比 Claude(8K) / GPT-4o(6K) / Cursor(10K) 都大 |
| USER.md | 20,000 字 | 用户画像充足 |
| 迁移阈值 | 85%（~34K 字） | 不到撑死就迁移 |
| 占上下文 | ~12% | DeepSeek 128K 的安全比例 |

**对比：**
- 当前（40K 字 / 10K tokens）→ 12% 上下文 → L2/L3 活 → 八层全转 ✅
- 设成 3MB（750K tokens）→ 587% 上下文 → L2/L3 死 → 八层变单层 ❌

## 网关重启与工具注册

在 `tools/` 目录新增工具文件后，工具在**网关重启**时才被框架发现和注册。重启会杀死当前会话。
如需在不中断会话的情况下注册工具，使用 cron 任务（`no_agent=true` + shell 脚本）安排延迟重启。

## 铁律（不可违背）

1. **不删除** — 任何记忆归档后永久保留
2. **不断链** — 迁移必须留短指针，指针必须能解析
3. **不丢标签** — 每份归档必须打至少一个标签，标签必须进索引
4. **自动闭环** — 每层触发不由人提醒，由规则自动驱动
5. **L0 不可触** — 只有爸能改 L0，自动机制无权修改
